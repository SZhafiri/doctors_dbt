# You can change 'my_dbt_project' to whatever your actual folder name is
dbt_dentist:
  target: dev
  outputs:
    dev:
      type: postgres
      host: postgres
      user: airflow
      password: airflow
      port: 5432
      dbname: airflow
      schema: public
      threads: 1